<resources>
    <style name="AppBaseTheme" parent="android:Theme.Light">
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
    	<item name="android:titleTextStyle">@style/TitleText</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
   	<!-- the theme applied to the application which contains the custom ActionBar Info -->
    <style name="CustomActionBarTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
		<item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
           parent="android:style/Widget.ActionBar">
		<item name="android:windowContentOverlay">@null</item>
        <item name="android:titleTextStyle">@style/TitleText</item>
        <item name="android:background">@color/ltGrey</item>
    </style>

    
        
    
    <color name="ltGrey">#DDDDDD</color>
       
    
    
    <style name="QuarterButton" parent="android:Theme.Light">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
    </style>
    
    <style name="TitleText" parent="android:style/TextAppearance">
    	<item name="android:ellipsize">end</item>
      	<item name="android:maxLines">1</item>
      	<item name="android:fontFamily">sans-serif-condensed</item>
      	<item name="android:textSize">22dp</item>
    </style>
    
    <style name="BorderlessButton" parent="android:Theme.Light">
        <item name="android:layout_width">wrap_content</item>
       	<item name="android:layout_height">wrap_content</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
    </style>

</resources>
